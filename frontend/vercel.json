{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...slug]?slug=$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "functions": {
    "api/[...slug].js": {
      "maxDuration": 30
    }
  }
}
